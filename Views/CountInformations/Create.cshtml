@model WalmartInventory.Models.CountInformation
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CountInformation</h4>
<hr />
<div class="row font-italic text-success h3">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger  "></div>
            <div class="form-group">
                <label asp-for="ProductId" class="control-label h4"></label>
                <select asp-for="ProductId" class="form-control" asp-items="ViewBag.ProductId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CountId" class="control-label h4"></label>
                <select asp-for="CountId" class="form-control" asp-items="ViewBag.CountId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Onhand" class="control-label h4"></label>
                <input asp-for="Onhand" class="form-control" />
                <span asp-validation-for="Onhand" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success h4" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
